/*
 * Templatede Componentes: Se crea con Control + Alt + R: tsrfce 
 * (antes instalar el snipet ES7+React/Redux/React-Native de dsznajder)
**/
import React from 'react'

type Props = {}

function Form({ }: Props) {
    return (
        <div>Form</div>
    )
}

export default Form
